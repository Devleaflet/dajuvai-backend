node_modules
logger
.env
src/config/firebase-service-account.json

# Firebase service account keys — private keys, never commit.
# Broad patterns so a re-downloaded key with a new hash suffix stays ignored.
*firebase-adminsdk*.json
*serviceAccount*.json
firebase-service-account.json
